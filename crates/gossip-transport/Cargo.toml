[package]
name = "gossip-transport"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Transport traits for gossip protocol implementations"

[dependencies]
gossip-core = { path = "../gossip-core" }
